<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<style>
* { 
    margin: 0; 
    padding: 0; 
    box-sizing: border-box; 
}

body {
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #000;
    overflow: hidden;
    font-family: 'Inter', sans-serif;
}

#bg-video {
    position: fixed;
    top: 0; left: 0;
    width: 100vw; height: 100vh;
    object-fit: cover;
    z-index: -2;
    filter: brightness(0.3);
}

#snow {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: -1;
    pointer-events: none;
}

.tilt-container {
    perspective: 1000px;
    z-index: 10;
}

.tilt-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    transform-style: preserve-3d;
}

.rectangle {
    background: rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 25px;
    opacity: 0;
    transition: opacity 0.5s ease;
    width: 650px;
}

.rectangle.visible { opacity: 1; }

.rectangle.top {
    height: 110px;
    display: flex;
    align-items: center;
    padding: 0 30px;
    gap: 25px;
}

.rectangle.bottom {
    height: 450px; 
    padding: 12px;
}

.avatar-wrapper {
    position: relative;
    width: 65px; height: 65px;
}

.avatar {
    width: 100%; height: 100%;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.2);
}

.santa-hat {
    position: absolute;
    top: -60px;
    left: -35px;
    width: 105px;
    z-index: 5;
    pointer-events: none;
    transform: rotate(-15deg);
}

.avatar, .profile-text { pointer-events: none; }
.profile-text h1 { font-size: 1.6rem; color: white; }
.profile-text p { font-size: 0.85rem; color: rgba(255,255,255,0.5); }

iframe { width: 100%; height: 100%; border-radius: 18px; border: none; }

.overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0,0,0,0.95); z-index: 9999; display: flex; justify-content: center; align-items: center;
    transition: opacity 0.6s ease;
}
.overlay.fade-out { opacity: 0; pointer-events: none; }
.overlay p { color: white; font-size: 1.8rem; font-family: 'Shadows Into Light'; }
</style>
</head>
<body>

<video autoplay muted loop playsinline id="bg-video">
    <source src="https://files.catbox.moe/0wtn38.mp4" type="video/mp4">
</video>

<canvas id="snow"></canvas>

<div class="overlay" id="overlay" onclick="startSite()">
    <p>𝘊𝘓𝘐𝘊𝘒</p>
</div>

<div class="tilt-container">
    <div class="tilt-group">
        <div class="rectangle top">
            <div class="avatar-wrapper">
                <img class="santa-hat" src="https://i.postimg.cc/bJdjKbNc/48099aa9afa80cbc8a966469a1d6da7f-removebg-preview.png" alt="hat">
                <img class="avatar" src="https://i.postimg.cc/bYDT3TfZ/image-(1).webp">
            </div>
            <div class="profile-text">
                <h1>𝒩'</h1>
                <p id="typewriter"></p>
            </div>
        </div>
        <div class="rectangle bottom">
            <iframe src="https://open.spotify.com/embed/playlist/5okZK4f15ghxKY74zBnM9w?utm_source=generator" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"></iframe>
        </div>
    </div>
</div>

<script>
const canvas = document.getElementById('snow');
const ctx = canvas.getContext('2d');
let flakes = [];

function initSnow() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    flakes = [];
    for(let i=0; i<100; i++) {
        flakes.push({
            x: Math.random()*canvas.width,
            y: Math.random()*canvas.height,
            speed: Math.random()*0.5 + 0.2,
            size: Math.random()*2 + 1,
            wind: Math.random()*0.5 - 0.25
        });
    }
}

function drawSnow() {
    ctx.clearRect(0,0,canvas.width, canvas.height);
    ctx.fillStyle = "rgba(255, 255, 255, 0.6)";
    flakes.forEach(f => {
        ctx.beginPath();
        ctx.arc(f.x, f.y, f.size, 0, Math.PI*2);
        ctx.fill();
        f.y += f.speed;
        f.x += f.wind;
        if(f.y > canvas.height) f.y = -5;
        if(f.x > canvas.width) f.x = 0;
        if(f.x < 0) f.x = canvas.width;
    });
    requestAnimationFrame(drawSnow);
}

initSnow(); drawSnow(); window.addEventListener('resize', initSnow);

function startSite() {
    document.getElementById('overlay').classList.add('fade-out');
    document.querySelectorAll('.rectangle').forEach(r => r.classList.add('visible'));
    const text = "09/12/2010";
    let i = 0;
    const interval = setInterval(() => {
        document.getElementById("typewriter").textContent += text.charAt(i); i++;
        if(i >= text.length) clearInterval(interval);
    }, 80);
}

const titleText = ["@","@N ", "@No ", "@Noé ", "@No","@N","@"];
let ti = 0;
setInterval(() => { document.title = titleText[ti]; ti = (ti + 1) % titleText.length; }, 400);
document.getElementById('bg-video').playbackRate = 0.3;
</script>

</body>
</html>
